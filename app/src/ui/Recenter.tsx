import React from "react";
import Button from "./components/Button";

export default function Recenter() {
    return <></>
    return <div className={"majorElement no-border"} id={"recenter"}>
        <Button className={"blue"}>Recenter</Button>
    </div>
}